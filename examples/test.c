#include "test.h"
int32 get_number(struct slice i) {
return i;
}
int32 main() {
int32 arr[4] = {1, 2, 3, 4};
int64[] tmp = { .ptr = arr, .len = sizeof(arr)/sizeof(arr[0])};
get_number(tmp);
int32 c = 5;
}
